### 7.2 IO接口

**01.** 在统一编址的方式下，区分存储单元和 LO 设备是靠 ( ) .。

- A. 不同的地址码
- B. 不同的地址线
- C. ，不同的控制线
- D. ， 不同的数据线

---

**03.** 下列功能中，属于 LO 接口的功能的是( ) 。 工 数据格式的转换           开 IO 过程中错误与状态检测 II.，IO 操作的控制与定时        IV.， 与主机和外设通信

- A. 工和TV
- B. 贡 II和TIV
- C. [IT和TV
- D. 1 IE IE和1IV

---

**04.** 下列关于 IO 端口和接口的说法中，正确的是( ) 。

- A. 在统一编址方式下，对主存单元和J/O 端口的存储保存措施是独立的
- B. 在统一编址方式下，主存单元和JI/O 端口是得不同的地址线来区分的
- C. ， 在独立编址方式下，主存单元和1/O 端口是等不同的地址线来区分的
- D. 在独立编址方式下，CPU 需要设置专门的输入/输出指令访问 IO 端口

---

**05.** 下列属于 LO 接口中寄存器的有 ( ) 。 工 指令寄存器        II 控制寄存器    JI，状态寄存器 V， 数据缓冲寄存器     IV，存储器地址寄存器

- A. TIIIIT和V
- B. IIIT和IV CI HHE和V  ，
- D. IIIIV和V

---

**06.** IO 的编址方式采用统一编址方式时，进行输入/输出的操作的指令是( ) 。

- A. 控制指令
- B. 访存指令
- C. 给入/输出指令 “
- D. 都不对

---

**07.** 下列关于 LIO 指令的说法中，错误的是( ) 。

- A. J/O 指令是 CPU 系统指令的一部分 BIO 指令是机器指令的一类 CI/O 指令反映 CPU 和 IJ/O 设备交换信息的特点
- D. ，J/O 指令的格式和通用指令的格式相同

---

**08.** 下列叙述中，正确的是( ) 。

- A. 只有 IO 指令可以访问 IO 设备
- B. 在统一编址下，不能直接访问 UO 设备
- C. ， 访问存储器的指令一定不能访问 IO 设备
- D. 只有在具有专门 LO 指令的计算机中，JLO 设备才可以单独编址

---

**09.** 在内存地址空间与接口地址空间统一编址的计算机中，不需要的指令是( ) 。

- A. 数据传送类 (如MOV 指令)
- B. 算术、逻辑运算类 (如ADD、 SP AND 和OR 指令)
- C. ，输入/输出类 (如IN 和OUT 指令
- D. 程序控制类 ( 如条件转移指令: 让字条调用四全

---

**10.** 在统一编址的情况下，就 IO 设备而言，其对应的 吧 地址不可取的是( ) 。

- A. 要求固定在地址高端
- B. ， 要求固定在地址低端
- C. ， 要求相对固定在地址的某部分
- D. 可以随意在地址的任何地方

---

**11.** 磁盘驱动器向偶片磁道记录数据时采用 ( ) 方式写入。

- A. 并行
- B. 丫行
- C. 并行-串行
- D. ，串行-并行

---

**12.** 采用中断方式进行打印控制时，在打印控制接口和打印机之问交换的信息不包括 ( ) 。

- A. CPU 和主存一I/O 总线一通信和总线 (电缆 ) 一IO 接口一外设
- B. CPU 和主存一IO 总线一IO 接口一通信总线 (电缆) 一外设

---
